const suggestionForm = document.querySelector("#suggestionForm");

function handleSumbit (event){
  event.preventDefault();

  const formSuggestion = new formSuggestion(suggestionForm);
  const formSuggestions = Object.
}